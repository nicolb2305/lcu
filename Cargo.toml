[workspace]
members = [
    "client-api",
    "lol-utilities",
    "custom-game-creator",
    "custom-game-randomizer",
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
# panic = "abort"
